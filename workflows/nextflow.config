// Nextflow configuration for workflows subdirectory
// This ensures that workflows run directly from this directory
// still use the main repository configuration

// Include the parent directory's configuration
includeConfig '../nextflow.config'
